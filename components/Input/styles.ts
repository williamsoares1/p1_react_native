import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    label: {
        width: 120,
        backgroundColor: '#fff',
        fontWeight: 'bold',
        textAlign: 'center',
        color: '#757575',
        marginLeft: 15,
        position: 'relative',
        top: 13,
        zIndex: 1
    },
    input: {
        borderColor: '#d1d1d1',
        borderWidth: 1.5,
        borderRadius: 7,
        fontSize: 20,
        padding: 22,
    }
})

export default styles;