import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    lastText: {
        color: '#757575',
        marginTop: 20,
        marginBottom: 70,
        textAlign: 'center',
        fontSize: 16,
        fontWeight: '400',
    },
    redirectText: {
        color: '#3382EB',
        fontWeight: '700'
    }
})

export default styles;